import directory_test

if __name__ == "__main__":
    # Create the window
    window = createWindow()
    
    # Start flashing colors
    flash_colors(window)

    # Run the Tkinter event loop
    window.mainloop()